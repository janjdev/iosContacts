import enum ObjectLibrary.InputField
import enum ObjectLibrary.State

extension InputField {
    func isValid(value: String) -> Bool {
        /**
         // TODO: - Implement this method to validate the 'value' parameter based upon the requirements listed in `README.md`
         -
         // HINT: - For attributes that are required only to contain letters (`Aa-Zz`) and decimal digits (`0-9`):
         -
         - Experiment with using `struct Foundation.CharacterSet` to validate the contents of a `String`.
         */
        return false
    }
}
